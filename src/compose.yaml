services:
    laravel.test:
        build:
            context: './vendor/laravel/sail/runtimes/8.5'
            dockerfile: Dockerfile
            args:
                WWWGROUP: '${WWWGROUP}'
        image: 'sail-8.5/app'
        extra_hosts:
            - 'host.docker.internal:host-gateway'
        ports:
            - '${APP_PORT:-80}:80'
            - '${VITE_PORT:-5173}:${VITE_PORT:-5173}'
        environment:
            WWWUSER: '${WWWUSER}'
            LARAVEL_SAIL: 1
            SUPERVISOR_PHP_USER: 'root'
            XDEBUG_MODE: '${SAIL_XDEBUG_MODE:-off}'
            XDEBUG_CONFIG: '${SAIL_XDEBUG_CONFIG:-client_host=host.docker.internal}'
            IGNITION_LOCAL_SITES_PATH: '${PWD}'
            DB_MIGRATE: '${DB_MIGRATE:-false}'
            DB_SEED: '${DB_SEED:-false}'
        command:
            - bash
            - -c
            - |
                if [ "$$DB_MIGRATE" = 'true' ]; then
                    echo 'Running database migrations...'
                    php artisan migrate --force
                fi
                if [ "$$DB_SEED" = 'true' ]; then
                    echo 'Seeding database...'
                    php artisan db:seed --class=RequiredDatabaseSeeder --force
                fi
                exec /usr/bin/supervisord -c /etc/supervisor/conf.d/supervisord.conf
        volumes:
            - '.:/var/www/html'
        networks:
            - sail
        depends_on:
            mysql:
                condition: service_healthy
            redis:
                condition: service_healthy
            mailpit:
                condition: service_started
    mysql:
        image: 'mysql:8.4'
        ports:
            - '${FORWARD_DB_PORT:-3306}:3306'
        environment:
            MYSQL_ROOT_PASSWORD: '${DB_PASSWORD}'
            MYSQL_ROOT_HOST: '%'
            MYSQL_DATABASE: '${DB_DATABASE}'
            MYSQL_USER: '${DB_USERNAME}'
            MYSQL_PASSWORD: '${DB_PASSWORD}'
            MYSQL_ALLOW_EMPTY_PASSWORD: 1
            MYSQL_EXTRA_OPTIONS: '${MYSQL_EXTRA_OPTIONS:-}'
        volumes:
            - 'sail-mysql:/var/lib/mysql'
            - './vendor/laravel/sail/database/mysql/create-testing-database.sh:/docker-entrypoint-initdb.d/10-create-testing-database.sh'
        networks:
            - sail
        healthcheck:
            test:
                - CMD
                - mysqladmin
                - ping
                - '-p${DB_PASSWORD}'
            retries: 3
            timeout: 5s
    redis:
        image: 'redis:alpine'
        ports:
            - '${FORWARD_REDIS_PORT:-6379}:6379'
        volumes:
            - 'sail-redis:/data'
        networks:
            - sail
        healthcheck:
            test:
                - CMD
                - redis-cli
                - ping
            retries: 3
            timeout: 5s
    mailpit:
        image: 'axllent/mailpit:latest'
        ports:
            - '${FORWARD_MAILPIT_PORT:-1025}:1025'
            - '${FORWARD_MAILPIT_DASHBOARD_PORT:-8025}:8025'
        networks:
            - sail

    # ---------------------------------------------------------------------------
    # Plausible Analytics Community Edition (self-hosted) — OPTIONAL
    #
    # Uncomment this block to run a local Plausible instance alongside the app.
    # Requires:
    #   1. Uncomment the `plausible_db`, `plausible_events_db`, and
    #      `plausible` service blocks below.
    #   2. Create a file plausible-conf.env next to compose.yaml with at least:
    #        BASE_URL=http://localhost:8000
    #        SECRET_KEY_BASE=<64-char random string>
    #   3. Set PLAUSIBLE_API_URL=http://plausible:8000/api/event in your .env
    #      so the Laravel proxy forwards events to the local instance.
    #   4. Access the dashboard at http://localhost:8000
    #
    # plausible_db:
    #     image: 'postgres:16-alpine'
    #     restart: always
    #     volumes:
    #         - 'sail-plausible-db:/var/lib/postgresql/data'
    #     environment:
    #         POSTGRES_PASSWORD: postgres
    #     networks:
    #         - sail
    #     healthcheck:
    #         test: ['CMD-SHELL', 'pg_isready -U postgres']
    #         retries: 5
    #         timeout: 5s
    #
    # plausible_events_db:
    #     image: 'clickhouse/clickhouse-server:24-alpine'
    #     restart: always
    #     volumes:
    #         - 'sail-plausible-events-db:/var/lib/clickhouse'
    #     ulimits:
    #         nofile:
    #             soft: 262144
    #             hard: 262144
    #     networks:
    #         - sail
    #     healthcheck:
    #         test: ['CMD', 'wget', '--spider', '-q', 'http://localhost:8123/ping']
    #         retries: 5
    #         timeout: 5s
    #
    # plausible:
    #     image: 'ghcr.io/plausible/community-edition:v3'
    #     restart: always
    #     command: sh -c "sleep 10 && /entrypoint.sh db createdb && /entrypoint.sh db migrate && /entrypoint.sh run"
    #     depends_on:
    #         plausible_db:
    #             condition: service_healthy
    #         plausible_events_db:
    #             condition: service_healthy
    #     ports:
    #         - '${FORWARD_PLAUSIBLE_PORT:-8000}:8000'
    #     env_file:
    #         - plausible-conf.env
    #     environment:
    #         DATABASE_URL: 'postgres://postgres:postgres@plausible_db:5432/plausible_db'
    #         CLICKHOUSE_DATABASE_URL: 'http://plausible_events_db:8123/plausible_events'
    #     networks:
    #         - sail
    # ---------------------------------------------------------------------------

networks:
    sail:
        driver: bridge
volumes:
    sail-mysql:
        driver: local
    sail-redis:
        driver: local
    # sail-plausible-db:          # uncomment when enabling Plausible CE
    #     driver: local
    # sail-plausible-events-db:   # uncomment when enabling Plausible CE
    #     driver: local
